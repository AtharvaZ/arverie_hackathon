function test() {
  let H = 1000;
  // bottom = top + H.
  // window = 1000.
  // If top = -10, bottom = 990. 
  // 990 >= 1000 - 10 -> 990 >= 990 is TRUE!
  // If top = -20, bottom = 980.
  // 980 >= 990 is FALSE!
}
