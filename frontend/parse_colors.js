const fs = require('fs');
const http = require('http');

// Wait, node doesn't have native Canvas without installing canvas package.
// I'll skip extracting exact colors programmatically and just use my multimodal capabilities.
